import 'package:flutter/material.dart';

class MyCartWidget extends StatelessWidget {
  const MyCartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("My Cart Widget"),
    );
  }
}
